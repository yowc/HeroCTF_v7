# Project Configuration Tool

This is a simple command-line utility to manage settings for our internal applications.

## Features

* Read all key-value pairs from `settings.conf`.
* Append a new key-value pair to `settings.conf`.

## Usage

### Reading Configuration

To view all current settings:
```sh
./config-tool.py read